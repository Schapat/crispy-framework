# Project README

This project uses Crisp Harness for AI-agent collaboration.

Crisp Harness adds repository-level governance for turning human intent into
product docs, story packets, validation expectations, decisions, handoffs, and
implementation work. This README is a target-safe starting point; replace or
adapt it with the target project's real product overview when that truth exists.
If this is an existing project, preserve and document the local codebase,
scripts, tests, setup commands, and team conventions.

## Start Here

For agent work, read:

- `AGENTS.md`
- `docs/WORK_INTAKE.md`
- `docs/HARNESS.md`
- `docs/PROJECT_START.md` for new project/spec intake
- `docs/CONTINUITY.md` and `docs/handoffs/` for resumed work

## Local Project Truth

Target projects own their local harness after install. As the project evolves,
keep these files current:

- `docs/product/` for current product truth
- `docs/stories/` for story packets and evidence
- `docs/TEST_MATRIX.md` for behavior-to-proof mapping
- `docs/decisions/` for durable rationale
- `docs/HARNESS_BACKLOG.md` for missing harness capabilities

## Local Adaptation

This installed README is a canonical default, not a product description. Adapt
it after install to describe the target project, stack, setup commands,
validation commands, deployment notes, and team workflow.
